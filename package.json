{
  "name": "deep-agent-demo",
  "version": "1.0.0",
  "description": "基于 DeepSeek 实现通用型 AI 智能体",
  "type": "module",
  "scripts": {
    "build": "tsup",
    "dev": "npm run build && node dist/index.mjs",
    "demo:basic": "npm run build && node dist/demo-basic.mjs",
    "demo:search": "npm run build && node dist/demo-search.mjs",
    "demo:multi": "npm run build && node dist/demo-multi-agent.mjs"
  },
  "dependencies": {
    "openai": "^6.34.0",
    "dotenv": "^16.4.0",
    "fs-extra": "^11.2.0"
  },
  "devDependencies": {
    "@types/fs-extra": "^11.0.4",
    "@types/node": "^22.0.0",
    "tsup": "^8.5.1",
    "typescript": "^5.6.0"
  }
}